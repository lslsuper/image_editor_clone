# 测试计划 (Test Plan) - Image Editor Clone

## 1. 简介
本测试计划旨在为 Image Editor Clone 项目提供全面的质量保障策略。该项目是一个基于 AI 的图像编辑和生成工具，允许用户上传图片并使用自然语言提示词进行编辑。

## 2. 测试范围

### 2.1 功能测试
- **图片上传**: 验证文件类型（图片）、文件大小限制（10MB）、拖拽上传、多文件处理（虽然UI限制，但逻辑需健壮）。
- **参数设置**: 验证提示词输入、模型选择功能。
- **生成流程**: 验证“生成”按钮触发逻辑、Loading 状态显示、API 调用参数传递。
- **结果展示**: 验证生成图片的展示、错误提示处理。

### 2.2 性能测试
- **响应时间**: 界面交互响应时间（如文件选择后的预览延迟）。
- **资源占用**: 图片处理时的内存占用（前端）。
- *注：后端 API 依赖第三方 OpenRouter，性能主要受限于第三方，重点测试前端的超时和异步处理。*

### 2.3 安全测试
- **输入验证**: 防止恶意脚本注入（XSS）通过提示词。
- **文件安全**: 验证非图片文件的上传拦截。
- **API 安全**: 确保 API Key 不泄漏到客户端（环境变量检查）。

### 2.4 兼容性测试
- **浏览器兼容性**: 验证在主流浏览器（Chrome, Firefox, Safari, Edge）下的表现。
- **设备兼容性**: 响应式设计在桌面和移动设备上的布局。

## 3. 测试环境
- **硬件**: MacBook Pro / 虚拟 CI 环境
- **软件**: Node.js v20+, npm/pnpm
- **测试框架**: Vitest, React Testing Library
- **依赖服务**: OpenRouter API (测试环境使用 Mock)

## 4. 测试策略
- **单元测试**: 覆盖核心组件 (`Generator`) 和工具函数。
- **集成测试**: 覆盖 API 路由逻辑。
- **手动测试**: 针对 UI 细节和交互体验进行补充。

## 5. 准入/准出标准
- **准入**: 代码编译通过，无明显语法错误。
- **准出**: 所有 P0/P1 级测试用例通过率 100%，无严重 Bug 遗留。

## 6. 缺陷管理
- 缺陷等级：
    - **Blocker**: 系统崩溃，主要功能无法使用。
    - **Critical**: 核心功能错误，无变通方案。
    - **Major**: 功能错误，有变通方案。
    - **Minor**: UI/UX 问题，不影响功能。
